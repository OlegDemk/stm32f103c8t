/*
 * GSM_IOT_GA6.c
 *
 *  Created on: Oct 8, 2020
 *      Author: odemki
 */
#include "stdio.h"
#include "stm32f1xx_hal.h"

extern UART_HandleTypeDef huart1;

char AT_COMAND_CALL[]="ATD+";
char my_mobile_number[]="380931482354\r\n";

// Transmet commands
void call_from_GSM_module(void)
{
	uint8_t size=0;
	char str[40]={0};
	uint8_t size_mas=sizeof(str);
	memset(str, 0 , sizeof(str));

	// copy one string to another
	char *p = AT_COMAND_CALL + sizeof(AT_COMAND_CALL);
	strcpy(p, my_mobile_number);

	sprintf(str, p);

	int u=0;

//	sprintf(str,"ATD+my_mobile_number");      							// AT command
//	size=sizeof(str);
//	HAL_UART_Transmit(&huart1 , (uint8_t *)str, size, 0xFFFF);
}















//
